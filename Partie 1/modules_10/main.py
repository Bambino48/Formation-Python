"""
Les erreurs en python  – Les erreurs de syntaxes
                       — les erreurs d'executions
                       — les erreurs semantique
"""

# Les erreurs de syntaxes
""""
For i in range(10):
    print(i)
"""
# la sensibilite a la casse
# les deux-points
# L'utilisation de mot reserves
# Les guillemets

# Les erreurs d'execution (RuntimeError)
    # NameError (variable non definie)
    # TypeError

# Les erreurs semantiques (les erreurs de logiques)